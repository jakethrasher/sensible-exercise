`npm run test` to execute tests for `labelExtractor` and `rowExtractor`

Some example configurations were added to `src/index.ts`, the `main` function can be executed via ts-node and results will print to console